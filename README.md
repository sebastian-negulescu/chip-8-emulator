# Chip-8 Emulator

This is a simple emulator that executes the Chip-8 instruction set. 
It is built using C++, and can run a few Chip-8 games.

The only external library that it makes use of is 
```SDL2``` 
for graphics.
